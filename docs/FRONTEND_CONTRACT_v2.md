# Frontend Contract (v2)

- If not logged in: show Login/Register UI.
- If logged in: show message board + logout + "Logged in as ..."
- Only logged-in users can create posts.
- Posts list shows author name/email.

API base comes from `VITE_API_ORIGIN`.
